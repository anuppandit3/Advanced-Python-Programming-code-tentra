import pandas as pd

# list of n integers from user input
integer_list = list(map(int, input("").split()))

# Create a DataFrame with two columns: 'Integer' and 'Square'
df = pd.DataFrame({
    'Integer': integer_list,
    'Square': [x**2 for x in integer_list]
})

# Print the DataFrame
print(df)

